/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // if you use remote images, add domains here:
  // images: { domains: ["images.example.com"] },
};

export default nextConfig;
